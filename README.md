# Task Tracker Frontend

This is a React app skeleton for the Task Tracker.

Quickstart:
1. cd frontend
2. Install dependencies: `npm install` (or `yarn`)
3. Start dev server: `npm start`

Note: update `REACT_APP_API_URL` in `.env` or use default to point to backend.
